#!/usr/bin/env python3
"""
Hook script placeholder.
Messages are now delivered in real-time via socket.io, so this hook is no longer needed.
Kept for backwards compatibility with settings.json.
"""

import sys

def main():
    # No-op - messages are delivered in real-time now
    sys.exit(0)

if __name__ == '__main__':
    main()
