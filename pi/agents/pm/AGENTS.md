# Project Instructions

- Use the `send_msg` tool to send messages to team members
- Messages from other agents are delivered automatically between tool calls
- When idle, the system will wake you when new messages arrive
- Do NOT use bash to send messages — use the send_msg tool directly
- Keep responses concise and action-oriented
